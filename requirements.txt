# Core NLP
spacy>=3.7,<4.0

# Data processing
pandas>=2.0
numpy>=1.24

# Candidate generation (Phase 2)
rapidfuzz>=3.0              # fast fuzzy string matching for in-memory search
elasticsearch>=8.0,<9.0      # Elasticsearch backend (must match ES server version)

# LLM Disambiguation (Phase 4)
openai>=1.0                 # OpenAI-compatible API client (works with LMStudio)

# Testing
pytest>=7.0

# Optional: better biomedical tokenization (Phase 1)
# pip install scispacy
# python -m spacy download en_core_sci_sm
